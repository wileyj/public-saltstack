{
    "checks": {
        "puppet": {
            "command": "sudo /etc/sensu/plugins/check-mtime.rb -f /var/log/puppet/puppet.log -c 1200",
            "wiki": "http://wiki.something/for/check/failure",
            "interval": 60,
            "handlers": [ 
                "default",
                "slack"
            ],
            "subscribers": [
                "default"
            ]
        }
    }
}
