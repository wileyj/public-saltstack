{
    "api": {
        "host":     "<%= scope.lookupvar('api_host') %>",
        "port":     <%= scope.lookupvar('api_port') %>,
        "user":     "<%= scope.lookupvar('api_user') %>",
        "password": "<%= scope.lookupvar('api_password') %>"
    }
}
