{
    "mailer": {
        "mail_from": "<%= scope.lookupvar('mail_from') %>",
        "mail_to": "<%= scope.lookupvar('mail_to') %>",
        "smtp_address": "localhost",
        "smtp_port": "25",
        "subscriptions": {
            "vpn": {
                "mail_to": "<%= scope.lookupvar('mail_dev') %>"
            },
            "default": {
                "mail_to": "<%= scope.lookupvar('mail_alerts') %>"
            },
            "apache webservice": {
                "mail_to": "<%= scope.lookupvar('mail_alerts') %>"
            },
            "nginx webservice": {
                "mail_to": "<%= scope.lookupvar('mail_alerts') %>"
            }
        }
    }
}